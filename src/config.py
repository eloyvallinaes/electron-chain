from pathlib import Path

ROOT = Path(__file__).parent.parent
QUERY_DIR = ROOT / 'searches/queries'
DATA_DIR = ROOT / 'data'
UP_DIR = DATA_DIR / 'uniprot'
NCBI_DIR = DATA_DIR / 'ncbi'
SEARCH_DIR = ROOT / "searches"
HALO_DIR = SEARCH_DIR / "halocyanin"
